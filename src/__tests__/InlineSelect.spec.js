import { InlineSelect } from '../InlineSelect';


describe('<InlineSelect />', () => {
  test('Should be create field', () => {
    expect(InlineSelect.displayName).toBe('InlineField(select)');
  });
});
