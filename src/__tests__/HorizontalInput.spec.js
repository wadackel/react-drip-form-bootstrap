import { HorizontalInput } from '../HorizontalInput';


describe('<HorizontalInput />', () => {
  test('Should be create field', () => {
    expect(HorizontalInput.displayName).toBe('HorizontalField(input)');
  });
});
