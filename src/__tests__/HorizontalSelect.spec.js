import { HorizontalSelect } from '../HorizontalSelect';


describe('<HorizontalSelect />', () => {
  test('Should be create field', () => {
    expect(HorizontalSelect.displayName).toBe('HorizontalField(select)');
  });
});
